# Build output.
/target

# `tests/fixtures/generated/` and `tests/fixtures/.cyclone/` are deliberately
# committed: tests/generated.rs compiles that tree, and tests/cli.rs checks it
# is still what the generator writes today.
