// GENERATED BY cyclonec
// DO NOT EDIT MANUALLY
// source: src/models/player.h
// model: PlayerInfo
// codec: edge
// fingerprint: sha256:a432f144fb89b1ec9322324f27ddadb099f69de79fc561d03b7ecf45e7545168
// cyclonec-version: 0.2.0
// generated-at: 2026-08-13T16:56:17Z

#pragma once

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include <stdlib.h>
#include <string.h>

#include "runtime.h"
#include "src/models/player.h"

// The "edge" codec for PlayerInfo, generated from its Cyclone markers.
//
// The wire layout, in declaration order (RFC-0002 SS5.1):
//
//  0. `Level`: `u32`

static const char *const PlayerInfoEdgeCodec_MESSAGE_NAME = "PlayerInfo.edge";
static const uint32_t PlayerInfoEdgeCodec_MESSAGE_ID = 0xC61DC711u;
static const uint64_t PlayerInfoEdgeCodec_FINGERPRINT = 0xA432F144FB89B1ECULL;

// Writes the "edge" fields of `*value`, in declaration order. Returns `false` (buffer
// untouched beyond what was already written) if growing the writer's buffer failed.
static inline bool PlayerInfoEdgeCodec_encode(CycloneWriter *writer, const struct PlayerInfo *value) {
    if (!cyclone_writer_write_u32(writer, value->Level)) return false;
    return true;
}

// Reads the "edge" fields into `*value`, in declaration order.
//
// Fields this codec does not carry are left as they were, which is what lets one
// model be split across several codecs.
//
// A field the stream ended before takes its zero value (RFC-0002 SS9.1); a field
// the stream ended inside is an error. Bytes left over after the last field
// belong to a newer writer's model and are ignored.
//
// `*value` must not already hold data from a previous, unfreed decode - see
// runtime.h's module docs.
static inline CycloneDecodeError PlayerInfoEdgeCodec_decode(CycloneReader *reader, struct PlayerInfo *value) {
    if (cyclone_reader_field_absent(reader)) {
        value->Level = 0;
    } else {
        CycloneDecodeError error = cyclone_reader_read_u32(reader, &value->Level);
        if (!cyclone_decode_error_ok(&error)) return error;
    }
    return cyclone_decode_ok();
}
