// GENERATED BY cyclonec
// DO NOT EDIT MANUALLY
// source: src/models/player.h
// model: Player
// codec: unity
// fingerprint: sha256:61ceea4540133a5303c731226d226529d008b81ccf835171a07a47d2048be6d7
// cyclonec-version: 0.2.0
// generated-at: 2026-08-13T16:56:17Z

#pragma once

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include <stdlib.h>
#include <string.h>

#include "runtime.h"
#include "src/models/player.h"

// The "unity" codec for Player, generated from its Cyclone markers.
//
// The wire layout, in declaration order (RFC-0002 SS5.1):
//
//  0. `Id`: `u32`
//  1. `Name`: `string`

static const char *const PlayerUnityCodec_MESSAGE_NAME = "Player.unity";
static const uint32_t PlayerUnityCodec_MESSAGE_ID = 0xF4716BBEu;
static const uint64_t PlayerUnityCodec_FINGERPRINT = 0x61CEEA4540133A53ULL;

// Writes the "unity" fields of `*value`, in declaration order. Returns `false` (buffer
// untouched beyond what was already written) if growing the writer's buffer failed.
static inline bool PlayerUnityCodec_encode(CycloneWriter *writer, const struct Player *value) {
    if (!cyclone_writer_write_u32(writer, value->Id)) return false;
    if (!cyclone_writer_write_string(writer, value->Name)) return false;
    return true;
}

// Reads the "unity" fields into `*value`, in declaration order.
//
// Fields this codec does not carry are left as they were, which is what lets one
// model be split across several codecs.
//
// A field the stream ended before takes its zero value (RFC-0002 SS9.1); a field
// the stream ended inside is an error. Bytes left over after the last field
// belong to a newer writer's model and are ignored.
//
// `*value` must not already hold data from a previous, unfreed decode - see
// runtime.h's module docs.
static inline CycloneDecodeError PlayerUnityCodec_decode(CycloneReader *reader, struct Player *value) {
    if (cyclone_reader_field_absent(reader)) {
        value->Id = 0;
    } else {
        CycloneDecodeError error = cyclone_reader_read_u32(reader, &value->Id);
        if (!cyclone_decode_error_ok(&error)) return error;
    }
    if (cyclone_reader_field_absent(reader)) {
        value->Name = NULL;
    } else {
        CycloneDecodeError error = cyclone_reader_read_string(reader, &value->Name);
        if (!cyclone_decode_error_ok(&error)) return error;
    }
    return cyclone_decode_ok();
}
