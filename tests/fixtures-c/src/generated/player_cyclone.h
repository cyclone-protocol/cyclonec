// GENERATED BY cyclonec
// DO NOT EDIT MANUALLY
// source: src/models/player.h
// model: Player
// fingerprint: sha256:78ae9c022001028506e81a92f53e37fc06bc1774aeea6abfc84c509aac9a94c2
// cyclonec-version: 0.2.0
// generated-at: 2026-08-20T06:05:20Z

#pragma once

#include <stddef.h>
#include <stdlib.h>

#include "runtime.h"
#include "src/models/player.h"

// Releases every heap allocation a decoded struct Player owns: every `string`, `bytes`
// and `Array<T>` field, and every nested model field, recursively - whichever
// of Player's codecs actually populated them.
//
// Safe on a freshly zero-initialized struct Player (struct Player value = {0};), on one this
// model's decode functions have populated, or on one already freed - never on
// one still holding data from a *previous, unfreed* decode (see runtime.h's
// module docs).
static inline void Player_free(struct Player *value) {
    free((void *)value->Name);
    value->Name = NULL;
    cyclone_bytes_free(&value->Payload);
}
