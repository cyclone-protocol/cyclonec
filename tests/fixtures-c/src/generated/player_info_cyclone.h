// GENERATED BY cyclonec
// DO NOT EDIT MANUALLY
// source: src/models/player.h
// model: PlayerInfo
// fingerprint: sha256:d520365f3a499794c809996e539ef5204e413a728c47989985d2b62f3b554a84
// cyclonec-version: 0.2.0
// generated-at: 2026-08-13T16:56:17Z

#pragma once

#include <stddef.h>
#include <stdlib.h>

#include "runtime.h"
#include "src/models/player.h"

// Releases every heap allocation a decoded struct PlayerInfo owns: every `string`, `bytes`
// and `Array<T>` field, and every nested model field, recursively - whichever
// of PlayerInfo's codecs actually populated them.
//
// Safe on a freshly zero-initialized struct PlayerInfo (struct PlayerInfo value = {0};), on one this
// model's decode functions have populated, or on one already freed - never on
// one still holding data from a *previous, unfreed* decode (see runtime.h's
// module docs).
static inline void PlayerInfo_free(struct PlayerInfo *value) {
    (void)value;
}
