// GENERATED BY cyclonec
// DO NOT EDIT MANUALLY
// source: src/models/player.hpp
// model: PlayerInfo
// codec: edge
// fingerprint: sha256:a432f144fb89b1ec9322324f27ddadb099f69de79fc561d03b7ecf45e7545168
// cyclonec-version: 0.2.0
// generated-at: 2026-08-13T15:56:37Z

#pragma once

#include <cstddef>
#include <cstdint>
#include <string>
#include <vector>

#include "runtime.hpp"
#include "src/models/player.hpp"

namespace generated {

/// The "edge" codec for PlayerInfo, generated from its Cyclone markers.
///
/// The wire layout, in declaration order (RFC-0002 §5.1):
///
///  0. `Level`: `u32`
struct PlayerInfoEdgeCodec {
    /// This message's name: `Model.codec`.
    static constexpr const char* kMessageName = "PlayerInfo.edge";

    /// This message's stable id, derived from its name alone.
    static constexpr std::uint32_t kMessageId = 0xC61DC711u;

    /// This message's wire-contract fingerprint - the same value
    /// handshake.hpp publishes, and the one a peer compares against.
    static constexpr std::uint64_t kFingerprint = 0xA432F144FB89B1ECULL;

    /// Writes the "edge" fields of `value`, in declaration order.
    static void encode(Writer& writer, const ::models::PlayerInfo& value) {
        writer.write_u32(value.Level);
    }

    /// Reads the "edge" fields into `value`, in declaration order.
    ///
    /// Fields this codec does not carry are left as they were, which is what lets one
    /// model be split across several codecs.
    ///
    /// A field the stream ended before takes its zero value (RFC-0002 §9.1); a field
    /// the stream ended inside is an error. Bytes left over after the last field
    /// belong to a newer writer's model and are ignored.
    static DecodeError decode(Reader& reader, ::models::PlayerInfo& value) {
        if (reader.field_absent()) {
            value.Level = 0;
        } else {
            if (DecodeError error = reader.read_u32(value.Level); !error.ok()) return error;
        }
        return DecodeError{};
    }
};

}  // namespace generated
