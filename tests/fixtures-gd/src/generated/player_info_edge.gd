# GENERATED BY cyclonec
# DO NOT EDIT MANUALLY
# source: src/models/player_info.gd
# model: PlayerInfo
# codec: edge
# fingerprint: sha256:b34420c17eefd973444bde6b7fc4d7dfebe996061c162872d402a0b8d9fc9a2c
# cyclonec-version: 0.2.0
# generated-at: 2026-08-20T06:05:20Z

class_name PlayerInfoEdgeCodec

# PlayerInfoEdgeCodec is the "edge" codec for PlayerInfo, generated from its Cyclone directives.
#
# The wire layout, in declaration order (RFC-0002 §5.1):
#
#  0. `level`: `u32`

# This message's name: Model.codec.
const MESSAGE_NAME := "PlayerInfo.edge"

# This message's stable id, derived from its name alone.
const MESSAGE_ID: int = 0xC61DC711

# This message's wire-contract fingerprint - the same value handshake.gd publishes,
# and the one a peer compares against.
const FINGERPRINT: int = (0xB34420C1 << 32) | 0x7EEFD973

# Writes the "edge" fields of value, in declaration order.
static func encode(writer: CycloneRuntime.Writer, value: PlayerInfo) -> void:
	writer.write_u32(value.level)

# Reads the "edge" fields into value, in declaration order.
#
# Fields this codec does not carry are left as they were, which is what lets one
# model be split across several codecs.
#
# A field the stream ended before takes its zero value (RFC-0002 §9.1); a field
# the stream ended inside is an error. Bytes left over after the last field
# belong to a newer writer's model and are ignored.
static func decode(reader: CycloneRuntime.Reader, value: PlayerInfo) -> CycloneRuntime.DecodeError:
	if reader.field_absent():
		value.level = 0
	else:
		var level_result = reader.read_u32()
		if level_result[1] != null:
			return level_result[1]
		value.level = level_result[0]
	return null
