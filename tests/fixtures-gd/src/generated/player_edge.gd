# GENERATED BY cyclonec
# DO NOT EDIT MANUALLY
# source: src/models/player.gd
# model: Player
# codec: edge
# fingerprint: sha256:231dd2d8744fecc3198c9853ffafe18023c93670fe7822c4cd9638fe9eabbe8b
# cyclonec-version: 0.2.0
# generated-at: 2026-08-13T13:15:40Z

class_name PlayerEdgeCodec

# PlayerEdgeCodec is the "edge" codec for Player, generated from its Cyclone directives.
#
# The wire layout, in declaration order (RFC-0002 §5.1):
#
#  0. `id`: `u32`
#  1. `x`: `f32`
#  2. `y`: `f32`

# This message's name: Model.codec.
const MESSAGE_NAME := "Player.edge"

# This message's stable id, derived from its name alone.
const MESSAGE_ID: int = 0x432AB486

# This message's wire-contract fingerprint - the same value handshake.gd publishes,
# and the one a peer compares against.
const FINGERPRINT: int = (0x231DD2D8 << 32) | 0x744FECC3

# Writes the "edge" fields of value, in declaration order.
static func encode(writer: CycloneRuntime.Writer, value: Player) -> void:
	writer.write_u32(value.id)
	writer.write_f32(value.x)
	writer.write_f32(value.y)

# Reads the "edge" fields into value, in declaration order.
#
# Fields this codec does not carry are left as they were, which is what lets one
# model be split across several codecs.
#
# A field the stream ended before takes its zero value (RFC-0002 §9.1); a field
# the stream ended inside is an error. Bytes left over after the last field
# belong to a newer writer's model and are ignored.
static func decode(reader: CycloneRuntime.Reader, value: Player) -> CycloneRuntime.DecodeError:
	if reader.field_absent():
		value.id = 0
	else:
		var id_result = reader.read_u32()
		if id_result[1] != null:
			return id_result[1]
		value.id = id_result[0]
	if reader.field_absent():
		value.x = 0.0
	else:
		var x_result = reader.read_f32()
		if x_result[1] != null:
			return x_result[1]
		value.x = x_result[0]
	if reader.field_absent():
		value.y = 0.0
	else:
		var y_result = reader.read_f32()
		if y_result[1] != null:
			return y_result[1]
		value.y = y_result[0]
	return null
